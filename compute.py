def main_compute(mode, data):
    if mode == 0:
        standard_compute(data)
    if mode == 1:
        random_compute(data)


def standard_compute(data):
    pass


def random_compute(data):
    pass
